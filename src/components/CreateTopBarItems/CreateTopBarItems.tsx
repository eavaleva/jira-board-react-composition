import {SearchBar} from "../SearchBar";
import { ProfileMenu} from "../ProfileMenu";

export const CreateTopBarItems = () => {
    return (
    <div className ="create-top-bar-items">
        <button className="create-button">Create</button>
    </div>
    );
};
